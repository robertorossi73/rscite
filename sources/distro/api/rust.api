String::from([str])    Crea una nuova stringa derivandola da str
String::new() -> String    Creates a new empty String
String::with_capacity(capacity: usize) -> String    Creates a new empty String with at least the specified capacity
String::from_utf8(vec: Vec<u8>) -> Result<String, FromUtf8Error>    Converts a vector of bytes to a String
String::from_utf8_lossy(v: &[u8]) -> Cow<'_, str>    Converts a slice of bytes to a string, including invalid characters
String::from_utf16(v: &[u16]) -> Result<String, FromUtf16Error>    Decode a native endian UTF-16–encoded vector v into a String
String::from_utf16_lossy(v: &[u16]) -> String    Decode a native endian UTF-16–encoded slice v into a String
String::from_raw_parts(buf: *mut u8,length: usize,capacity: usize,) -> String    Creates a new String
String::from_utf8_unchecked(bytes: Vec<u8>) -> String    Converts a vector of bytes to a String without checking that the string contains valid UTF-8
println!(...)    Prints to the standard output, with a newline.